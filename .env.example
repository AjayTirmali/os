# Optional. The OS Lab app is fully client-side and works without any database.
# This variable only powers the optional /api/health route (PostgreSQL via Drizzle).
# DATABASE_URL=postgresql://user:password@localhost:5432/oslab
