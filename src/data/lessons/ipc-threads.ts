import type { Lesson } from "../types";

export const IPC_THREAD_LESSONS: Lesson[] = [
  {
    id: "independent-vs-cooperating",
    section: "ipc",
    title: "Independent vs Cooperating Processes",
    shortDescription: "Processes that ignore each other vs those that work together.",
    definition: "Independent processes cannot affect (or be affected by) other processes; cooperating processes share data and can influence one another.",
    whyItMatters: "Cooperation enables parallelism, modularity, and information sharing — but it also introduces the need for synchronization.",
    simple: "An independent process works alone and shares nothing. Cooperating processes share data and coordinate, like two workers passing materials on an assembly line.",
    technical: "Independent processes have no shared state, so their execution is deterministic relative to each other. Cooperating processes communicate via IPC (shared memory or message passing) and must synchronize access to shared data to avoid races.",
    internal: "The kernel provides IPC primitives (shared memory segments, pipes, message queues) that cooperating processes attach to. For shared memory, the kernel maps a common region into multiple address spaces.",
    keyPoints: [
      "Independent: no shared data, cannot affect others.",
      "Cooperating: share data, can affect one another.",
      "Cooperation requires synchronization.",
    ],
    commonMistakes: [
      "Assuming independent processes can still write the same memory (they cannot).",
    ],
    examTips: [
      "Define both terms, then give one example of each.",
    ],
  },
  {
    id: "cooperating-processes",
    section: "ipc",
    title: "Cooperating Processes",
    shortDescription: "Why processes cooperate.",
    definition: "Processes cooperate to share information, speed up computation, gain modularity, and improve convenience.",
    whyItMatters: "These four reasons are a standard exam point and explain why IPC exists at all.",
    simple: "Processes team up for good reasons: to share data, to divide a big job and finish faster, to keep code organized into parts, and because sometimes it's just more convenient.",
    technical: "The four canonical reasons are: (1) information sharing, (2) computation speedup (parallelism across CPUs), (3) modularity (dividing a system into processes), and (4) convenience (one user running many tasks).",
    internal: "The OS exposes mechanisms (shared memory, message passing) to make cooperation possible while preserving isolation between processes.",
    keyPoints: [
      "Information sharing.",
      "Computation speedup.",
      "Modularity.",
      "Convenience.",
    ],
    commonMistakes: [
      "Listing 'security' as a reason to cooperate (isolation actually argues the opposite).",
    ],
    examTips: [
      "Memorize the four reasons — this is a direct recall question.",
    ],
  },
  {
    id: "ipc-concept",
    section: "ipc",
    title: "Inter-Process Communication",
    shortDescription: "The two models: shared memory and message passing.",
    definition: "IPC is the set of mechanisms that let processes exchange data — primarily via shared memory or message passing.",
    whyItMatters: "IPC is how any multi-process system (browsers, servers, microkernels) coordinates work.",
    simple: "If processes want to talk, they either share a region of memory (shared memory) or send messages through the kernel (message passing).",
    technical: "Shared memory is fast but requires the processes to manage synchronization themselves. Message passing is safer and cleaner (the kernel handles the transfer) but carries more overhead due to copying and system calls.",
    internal: "For shared memory, the kernel maps a common segment into both address spaces and then steps back. For message passing, the kernel copies data between the sender's and receiver's address spaces (or via a kernel buffer).",
    keyPoints: [
      "Two IPC models: shared memory and message passing.",
      "Shared memory: fast, but manual synchronization.",
      "Message passing: kernel-mediated, cleaner but more overhead.",
    ],
    commonMistakes: [
      "Claiming message passing is always faster than shared memory.",
    ],
    examTips: [
      "Compare the two models on speed, simplicity, and synchronization.",
    ],
    visualizer: "ipc",
  },
  {
    id: "shared-memory",
    section: "ipc",
    title: "Shared Memory",
    shortDescription: "A common memory region mapped into multiple processes.",
    definition: "Shared memory lets processes communicate by mapping a common memory region into their address spaces and reading/writing it directly.",
    whyItMatters: "It is the fastest IPC method because there is no kernel copying per exchange.",
    simple: "Both processes attach to the same chunk of memory. P1 writes a value; P2 reads it — no messages, no copying.",
    technical: "The OS maps one physical segment into the page tables of two or more processes. Once established, communication is just ordinary memory access — the kernel is not involved per operation. Processes must use synchronization primitives to avoid data races.",
    internal: "A process calls shmget/shmat (or mmap) to attach the segment; the kernel sets up shared page-table entries. Synchronization (mutexes/semaphores) is the programmers' responsibility.",
    keyPoints: [
      "Fast — no kernel copy per access.",
      "Kernel sets up the mapping, then stays out of the way.",
      "Requires explicit synchronization.",
      "Persists until explicitly removed.",
    ],
    commonMistakes: [
      "Forgetting that shared memory needs synchronization.",
    ],
    examTips: [
      "Mention 'no kernel intervention per access' as the key advantage.",
    ],
    visualizer: "ipc",
  },
  {
    id: "message-passing",
    section: "ipc",
    title: "Message Passing",
    shortDescription: "Exchanging messages through the kernel.",
    definition: "Message passing lets processes exchange data by send()/receive() operations mediated by the kernel, without sharing an address space.",
    whyItMatters: "It is the safer, more structured IPC model used in microkernels and distributed systems.",
    simple: "P1 packages data into a message and sends it; the kernel delivers it to P2, which receives it. No shared memory is needed.",
    technical: "Message passing requires at least two operations (send, receive) and a communication link. It can be direct (named processes) or indirect (via mailboxes/ports), and synchronous (blocking) or asynchronous (non-blocking). Data is copied by the kernel.",
    internal: "The kernel allocates a message buffer, copies the message from the sender's space, queues it at the receiver's mailbox, and copies it into the receiver's space on receive(). Each copy is overhead.",
    keyPoints: [
      "send()/receive() primitives.",
      "Direct or indirect (mailbox) addressing.",
      "Synchronous or asynchronous.",
      "Kernel copies data (overhead).",
    ],
    commonMistakes: [
      "Thinking message passing needs shared memory.",
    ],
    examTips: [
      "Be ready to contrast direct vs indirect and blocking vs non-blocking.",
    ],
    visualizer: "ipc",
  },
  {
    id: "direct-vs-indirect",
    section: "ipc",
    title: "Direct vs Indirect Communication",
    shortDescription: "Naming the receiver vs using a mailbox.",
    definition: "Direct communication names the exact sender/receiver (send(P2, msg)); indirect communication routes messages through a shared mailbox or port.",
    whyItMatters: "This determines coupling between processes — direct links are rigid, indirect links are flexible.",
    simple: "Direct: 'send this to P2 specifically.' Indirect: 'drop this in mailbox 7' — whoever reads mailbox 7 gets it.",
    technical: "Direct communication establishes a link between exactly two processes and requires mutual naming. Indirect communication lets multiple processes send to/receive from a mailbox; a mailbox is shared only by processes with access rights.",
    internal: "For direct links the kernel delivers to the target PID's queue. For mailboxes the kernel maintains a per-mailbox message queue and a list of blocked receivers.",
    keyPoints: [
      "Direct: explicit sender/receiver naming.",
      "Indirect: shared mailbox/port.",
      "Mailboxes allow many-to-one and one-to-many.",
    ],
    commonMistakes: [
      "Saying a direct link can have more than two processes.",
    ],
    examTips: [
      "Note that a direct link is '2 processes exactly'; a mailbox can be shared.",
    ],
  },
  {
    id: "synchronous-vs-asynchronous",
    section: "ipc",
    title: "Synchronous vs Asynchronous Communication",
    shortDescription: "Blocking vs non-blocking send/receive.",
    definition: "Synchronous (blocking) communication makes the sender wait until the message is received; asynchronous (non-blocking) lets the sender continue immediately.",
    whyItMatters: "Blocking vs non-blocking is a design trade-off between simplicity and throughput.",
    simple: "Synchronous: you hand over a package and wait until the receiver signs for it. Asynchronous: you drop the package off and walk away.",
    technical: "Combinations exist: blocking send/blocking receive (rendezvous), non-blocking send/blocking receive, and both non-blocking (buffered). Blocking simplifies correctness; non-blocking improves concurrency but requires buffering.",
    internal: "For a blocking receive with no message, the kernel removes the receiver from the ready queue and blocks it until a send occurs. A non-blocking receive returns immediately (possibly with an error).",
    keyPoints: [
      "Synchronous = blocking (rendezvous).",
      "Asynchronous = non-blocking.",
      "Four possible send/receive combinations.",
    ],
    commonMistakes: [
      "Equating 'synchronous' with 'message passing' in general.",
    ],
    examTips: [
      "Describe the rendezvous (blocking both) case specifically.",
    ],
  },
  {
    id: "producer-consumer",
    section: "ipc",
    title: "Producer-Consumer Problem",
    shortDescription: "A shared bounded buffer between a producer and a consumer.",
    definition: "The producer-consumer problem is a classic synchronization scenario where a producer fills a bounded buffer and a consumer drains it, without overflowing or underflowing.",
    whyItMatters: "It is the canonical example of inter-process synchronization, and it appears throughout OS and concurrency coursework.",
    simple: "A chef (producer) plates dishes on a small counter (buffer) and a waiter (consumer) takes them. If the counter is full, the chef waits; if it's empty, the waiter waits.",
    technical: "The producer must block when the buffer is full; the consumer must block when it is empty. Without synchronization, both processes may update the buffer's counters concurrently and corrupt it (a race condition).",
    internal: "Semaphores (empty, full, mutex) enforce the rules: 'empty' counts free slots, 'full' counts filled slots, and 'mutex' protects the buffer index updates from concurrent access.",
    keyPoints: [
      "Bounded buffer of fixed size.",
      "Producer waits when full.",
      "Consumer waits when empty.",
      "Synchronization prevents race conditions.",
    ],
    commonMistakes: [
      "Forgetting to synchronize the buffer counter itself.",
    ],
    examTips: [
      "Be able to write the three-semaphore structure (empty, full, mutex).",
    ],
    visualizer: "producer-consumer",
  },
  {
    id: "buffering",
    section: "ipc",
    title: "Buffering",
    shortDescription: "Zero, bounded, and unbounded message buffers.",
    definition: "Buffering decouples a message sender from the receiver by holding messages in a queue of some capacity.",
    whyItMatters: "Buffer capacity determines whether senders block and how much concurrency is possible.",
    simple: "A buffer is a holding area for messages: a zero-capacity buffer forces sender and receiver to meet; a bounded buffer holds a limited number; an unbounded buffer never makes the sender wait.",
    technical: "Zero capacity = rendezvous (sender blocks until receiver receives). Bounded capacity = a fixed queue (sender blocks when full). Unbounded capacity = effectively infinite (sender never blocks).",
    internal: "The kernel allocates the message queue, tracks occupancy, and blocks senders/receivers by moving them between ready and waiting queues as the buffer fills or empties.",
    keyPoints: [
      "Zero capacity: rendezvous.",
      "Bounded capacity: fixed-size queue.",
      "Unbounded capacity: sender never blocks.",
    ],
    commonMistakes: [
      "Thinking unbounded capacity is always practical (memory is finite).",
    ],
    examTips: [
      "Classify a scenario by its buffer capacity and describe blocking behavior.",
    ],
    visualizer: "producer-consumer",
  },
  {
    id: "thread-concept",
    section: "threads",
    title: "Thread Concept",
    shortDescription: "A lightweight unit of execution within a process.",
    definition: "A thread is a single flow of control within a process — it has its own program counter, registers, and stack, but shares the process's code, data, and files.",
    whyItMatters: "Threads give parallelism and responsiveness without the heavy cost of separate processes.",
    simple: "A process is like a house; threads are the people working inside it. They share the house's rooms (memory) but each has their own tools (stack and registers).",
    technical: "Each thread has a thread ID, a program counter, a register set, and a stack. Threads of the same process share the code section, data section, and open files. Thread creation and switching are much cheaper than for processes.",
    internal: "The kernel (or a user library) maintains per-thread state: a small PCB-like structure holding the thread's registers and stack pointer. Thread context switches only swap registers and stack — not the address space.",
    keyPoints: [
      "Thread = single flow of control.",
      "Own: PC, registers, stack, thread ID.",
      "Shared: code, data, open files.",
      "Cheaper to create/switch than processes.",
    ],
    commonMistakes: [
      "Thinking each thread has its own address space.",
    ],
    examTips: [
      "Contrast thread-owned vs process-shared resources — a favourite question.",
    ],
    visualizer: "threads",
  },
  {
    id: "process-vs-thread",
    section: "threads",
    title: "Process vs Thread",
    shortDescription: "Heavyweight vs lightweight.",
    definition: "A process is a heavyweight unit with its own address space; a thread is a lightweight execution unit that shares its process's address space.",
    whyItMatters: "The process/thread distinction is the most common threads exam question.",
    simple: "A process is a whole program with its own memory; a thread is a lane of execution inside it. Switching processes is expensive; switching threads is cheap.",
    technical: "Processes are isolated (separate address spaces, require IPC to communicate). Threads share memory directly, which is fast but requires synchronization. Thread creation/context-switching cost less than process creation/context-switching.",
    internal: "A process context switch reloads the address space (page tables, TLB flush). A thread context switch keeps the address space and only swaps registers and stack.",
    keyPoints: [
      "Process: own address space; isolated.",
      "Thread: shares address space; fast communication.",
      "Process switch is costlier than thread switch.",
    ],
    commonMistakes: [
      "Saying threads are always faster (isolation has real benefits too).",
    ],
    examTips: [
      "Prepare a difference table — it is almost always asked.",
    ],
    differences: {
      title: "Process vs Thread",
      headers: ["Process", "Thread"],
      rows: [
        ["Heavyweight", "Lightweight"],
        ["Own address space", "Shares the process's address space"],
        ["Communication via IPC", "Communication via shared memory"],
        ["Creation/switch is expensive", "Creation/switch is cheap"],
        ["Independent & isolated", "Depends on its process"],
      ],
    },
    visualizer: "threads",
  },
  {
    id: "thread-components",
    section: "threads",
    title: "Thread Components",
    shortDescription: "What belongs to a thread vs the process.",
    definition: "Each thread owns a program counter, register set, and stack; it shares the process's code, data, heap, and open files.",
    whyItMatters: "Knowing exactly what is shared vs private is the key to reasoning about thread bugs.",
    simple: "Your own tools (register values, program counter, stack) are private to each thread; the shared workspace (code, global data, files) belongs to the process.",
    technical: "Per-thread: thread ID, program counter, registers, stack, and a signal mask. Per-process (shared): code section, data section, heap, open file descriptors, and the address space itself.",
    internal: "The thread library/kernel allocates a private stack per thread and stores the thread's registers in a thread context block; global state lives in the shared process data/heap sections.",
    keyPoints: [
      "Private: PC, registers, stack, thread ID.",
      "Shared: code, data, heap, open files.",
    ],
    commonMistakes: [
      "Assigning the heap to individual threads (it is shared).",
    ],
    examTips: [
      "Remember: stack is per-thread; heap is per-process.",
    ],
    visualizer: "threads",
  },
  {
    id: "benefits-of-threads",
    section: "threads",
    title: "Benefits of Threads",
    shortDescription: "Responsiveness, resource sharing, economy, scalability.",
    definition: "Threads improve responsiveness, allow resource sharing, are economical to create, and scale across processors.",
    whyItMatters: "These four benefits justify why multithreading dominates modern programming.",
    simple: "Threads keep apps responsive (one thread handles the UI while another downloads), let tasks share data cheaply, are cheap to create, and can run on multiple cores at once.",
    technical: "The four canonical benefits: (1) responsiveness, (2) resource sharing (shared memory/file access), (3) economy (thread creation is far cheaper than process creation), and (4) scalability/utilization of multicore architectures.",
    internal: "Thread creation allocates only a stack and a small context block, avoiding the page-table setup and memory allocation of process creation.",
    keyPoints: [
      "Responsiveness.",
      "Resource sharing.",
      "Economy.",
      "Scalability on multicore CPUs.",
    ],
    commonMistakes: [
      "Forgetting 'economy' — thread creation is much cheaper than process creation.",
    ],
    examTips: [
      "List all four benefits; 'scalability/multicore utilization' is the modern emphasis.",
    ],
  },
  {
    id: "user-level-threads",
    section: "threads",
    title: "User-Level Threads",
    shortDescription: "Threads managed by a user-space library.",
    definition: "User-level threads are managed entirely by a thread library in user space, invisible to the kernel.",
    whyItMatters: "They are extremely fast to create/switch, but one blocking call can stall the whole process.",
    simple: "The program itself manages its threads using a library — the OS doesn't know the threads exist. That makes switching super fast, but if one thread blocks on I/O, the OS blocks the whole process.",
    technical: "The thread library schedules threads onto the process's (single) kernel thread. Thread switching requires no kernel mode transition and no system call. But a blocking system call blocks the entire process, and threads cannot run in parallel on multiple cores.",
    internal: "A user scheduler (in the library) saves/restores thread contexts by swapping stack pointers and register files. The kernel only ever schedules the one process (kernel thread).",
    keyPoints: [
      "Managed by a user library; kernel is unaware.",
      "No kernel mode switch → very fast.",
      "A blocking call blocks the whole process.",
      "No true parallelism (one kernel thread).",
    ],
    commonMistakes: [
      "Thinking user threads can run on multiple cores in the many-to-one case.",
    ],
    examTips: [
      "The classic disadvantage: one blocking call blocks all threads.",
    ],
  },
  {
    id: "kernel-level-threads",
    section: "threads",
    title: "Kernel-Level Threads",
    shortDescription: "Threads managed directly by the kernel.",
    definition: "Kernel-level threads are created, scheduled, and managed by the operating system kernel.",
    whyItMatters: "They give true parallelism and fine-grained blocking, at the cost of slower switching.",
    simple: "The OS manages each thread directly. That means threads can run on different cores in parallel and a blocked thread doesn't stop the others — but switching threads is slower because it needs a system call.",
    technical: "The kernel schedules threads independently, so one blocked thread doesn't block the process and threads can truly execute in parallel on multiple cores. Thread creation and switching require system calls, making them slower than user-level threads.",
    internal: "Each kernel thread has a kernel stack and a scheduling entry in the kernel's run queues. The dispatcher performs full thread context switches within the kernel.",
    keyPoints: [
      "Managed by the kernel.",
      "True parallelism across cores.",
      "One blocking thread doesn't block others.",
      "Slower due to system-call overhead.",
    ],
    commonMistakes: [
      "Saying kernel threads are always faster than user threads (switching is slower).",
    ],
    examTips: [
      "Contrast with user-level threads on speed, blocking, and parallelism.",
    ],
  },
  {
    id: "multithreading",
    section: "threads",
    title: "Multithreading",
    shortDescription: "Multiple threads inside one process.",
    definition: "Multithreading is the ability of a process to run multiple threads of execution concurrently within a single address space.",
    whyItMatters: "It is how modern servers, browsers, and apps achieve concurrency and parallelism.",
    simple: "One process doing several things at once — like a browser loading many tabs within one window.",
    technical: "Threads within a process share memory and resources, so communication is cheap but must be synchronized (mutexes, condition variables). On multicore systems, threads of a process can run on different cores simultaneously.",
    internal: "The scheduler treats each thread as a scheduling entity; threads of the same process can be spread across cores, and the kernel maintains per-thread state for saving/restoring.",
    keyPoints: [
      "Concurrent threads in one address space.",
      "Shared memory → cheap communication, needs locks.",
      "Enables parallelism on multicore CPUs.",
    ],
    commonMistakes: [
      "Ignoring the synchronization requirement of shared data.",
    ],
    examTips: [
      "Multithreading benefits vs risks (races) is a standard short answer.",
    ],
    visualizer: "threads",
  },
  {
    id: "multithreading-models",
    section: "threads",
    title: "Multithreading Models",
    shortDescription: "Many-to-One, One-to-One, Many-to-Many, Two-Level.",
    definition: "Multithreading models describe how user threads map to kernel threads.",
    whyItMatters: "The mapping determines parallelism, cost, and whether a blocking call stalls the process.",
    simple: "How do your program's threads connect to the OS's threads? Several mappings exist: all to one, one to one, many to many, or a hybrid.",
    technical: "Many-to-One maps many user threads to one kernel thread (cheap, but blocks on one syscall). One-to-One maps each user thread to its own kernel thread (parallel, but costly). Many-to-Many multiplexes many user threads over a pool of kernel threads. Two-Level adds binding (a user thread pinned to a kernel thread).",
    internal: "The thread library and kernel cooperate via a scheduler activation / LWP (lightweight process) abstraction to implement many-to-many mapping.",
    keyPoints: [
      "Many-to-One: many user → 1 kernel.",
      "One-to-One: each user → 1 kernel.",
      "Many-to-Many: many user → many kernel.",
      "Two-Level: many-to-many + binding.",
    ],
    commonMistakes: [
      "Confusing many-to-one with one-to-many.",
    ],
    examTips: [
      "Know which model each real OS uses (e.g., Linux & Windows: one-to-one).",
    ],
    visualizer: "thread-models",
  },
  {
    id: "thread-scheduling",
    section: "threads",
    title: "Thread Scheduling",
    shortDescription: "PCS and SCS contention scopes.",
    definition: "Thread scheduling is the process of deciding which thread runs; its scope is described by Process Contention Scope (PCS) or System Contention Scope (SCS).",
    whyItMatters: "The scope determines whether threads compete locally or system-wide.",
    simple: "PCS means threads compete with each other inside one process. SCS means the kernel schedules threads against every thread in the system.",
    technical: "With many-to-one and many-to-many models, the thread library does PCS (user threads compete for the process's kernel threads). In one-to-one, the kernel does SCS (each kernel thread competes globally). SCS gives true fairness across processes; PCS is faster but local.",
    internal: "PCS is implemented by a user-level scheduler switching user threads onto LWPs. SCS is implemented by the kernel's ready queue, where each kernel thread is a separate entry.",
    keyPoints: [
      "PCS: threads compete within a process.",
      "SCS: threads compete system-wide.",
      "Many-to-one/many-to-many → PCS (plus SCS for LWPs).",
      "One-to-one → SCS.",
    ],
    commonMistakes: [
      "Assuming PCS and SCS are unrelated to the threading model.",
    ],
    examTips: [
      "Link PCS/SCS to the multithreading model in your answer.",
    ],
  },
];
