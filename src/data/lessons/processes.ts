import type { Lesson } from "../types";

export const PROCESS_LESSONS: Lesson[] = [
  {
    id: "process-concept",
    section: "processes",
    title: "Process Concept",
    shortDescription: "The fundamental unit of work in a running system.",
    definition: "A process is a program in execution — an active entity consisting of code, data, and a current activity (program counter, registers, and stack).",
    whyItMatters: "Everything the OS does is about managing processes. Understanding what a process is underlies scheduling, memory, and I/O.",
    simple: "A program is just instructions sitting on disk. When you double-click it, the OS loads it into memory and starts running it — that running instance is a process. The same program can be running as many processes at once.",
    technical: "A process is a dynamic execution environment: it includes the text (code) section, data section (globals), heap, and stack, plus CPU registers and a program counter. Unlike a program (passive), a process is active and has a well-defined state and address space.",
    internal: "The loader reads the executable, allocates an address space, loads code and initialized data, initializes the stack, sets the program counter to the entry point, and hands control to the CPU. The kernel creates a PCB to track it.",
    keyPoints: [
      "A process = program + state + resources.",
      "Two processes from the same program are independent executions.",
      "A process has its own address space, unlike a program.",
      "The kernel tracks every process in a PCB.",
    ],
    commonMistakes: [
      "Treating 'program' and 'process' as synonyms.",
      "Assuming one program maps to exactly one process.",
    ],
    examTips: [
      "Define process as 'a program in execution' — that phrase is almost always the expected answer.",
      "Be ready to list the sections of a process image: text, data, heap, stack.",
    ],
    visualizer: "program-to-process",
  },
  {
    id: "program-vs-process",
    section: "processes",
    title: "Program vs Process",
    shortDescription: "Passive instructions vs active execution.",
    definition: "A program is a passive file on disk; a process is an active instance of that program loaded in memory and being executed.",
    whyItMatters: "Confusing the two is one of the most common conceptual errors in OS exams.",
    simple: "Think of a recipe (program) versus someone cooking the recipe (process). The recipe is static; cooking is dynamic. You can run the same recipe in two kitchens simultaneously.",
    technical: "A program exists only as instructions and data on disk. A process is created when the loader maps those instructions into memory, allocates a stack and heap, and the scheduler starts executing it. A single program can produce many processes.",
    internal: "The OS's loader performs the program→process transformation: allocating memory, setting up page tables, initializing registers, and creating a PCB entry.",
    keyPoints: [
      "Program: passive, on disk.",
      "Process: active, in memory, executing.",
      "One program → many possible processes.",
      "Process has a PID and state; program has neither.",
    ],
    commonMistakes: [
      "Saying a program 'runs' — only processes run.",
      "Believing each process needs its own program file.",
    ],
    examTips: [
      "A classic 'difference' question: contrast the two on activity, storage location, and lifetime.",
    ],
    differences: {
      title: "Program vs Process",
      headers: ["Program", "Process"],
      rows: [
        ["Passive entity (instructions on disk)", "Active entity (instructions in execution)"],
        ["Stored on secondary storage", "Lives in main memory"],
        ["No concept of state or PID", "Has a state and a unique PID"],
        ["Long-lived (until deleted)", "Short-lived (until terminated)"],
        ["One program", "Can produce many processes"],
      ],
    },
  },
  {
    id: "process-in-memory",
    section: "processes",
    title: "Process in Memory",
    shortDescription: "How a process's address space is laid out.",
    definition: "A process in memory is divided into four sections: text (code), data (globals), heap (dynamic memory), and stack (function calls and locals).",
    whyItMatters: "This layout explains where variables live and why stack overflows and heap allocation behave the way they do.",
    simple: "Each process gets its own slice of memory arranged in layers: code at the bottom, then global data, then a heap that grows upward for dynamically allocated objects, and a stack that grows downward for function calls.",
    technical: "The text section holds compiled machine code (often read-only). The data section holds initialized and uninitialized globals. The heap grows upward for malloc/new. The stack grows downward, one frame per function call, holding return addresses and local variables.",
    internal: "The kernel's memory manager sets up an address space with these segments mapped into page tables. The heap pointer (brk) and stack pointer (SP) delimit the dynamically growing regions; a guard page detects stack overflow.",
    keyPoints: [
      "Text = code (read-only).",
      "Data = global and static variables.",
      "Heap = dynamic allocation, grows up.",
      "Stack = function calls/locals, grows down.",
    ],
    commonMistakes: [
      "Thinking heap and stack are in the same region.",
      "Forgetting the data section exists separately from the stack.",
    ],
    examTips: [
      "Draw the 4-layer diagram: Stack / Heap / Data / Code.",
      "Mention growth directions — heap up, stack down — a favourite exam detail.",
    ],
    visualizer: "program-to-process",
  },
  {
    id: "process-states",
    section: "processes",
    title: "Process States",
    shortDescription: "New, Ready, Running, Waiting, Terminated.",
    definition: "A process is always in exactly one of five states: New, Ready, Running, Waiting, or Terminated.",
    whyItMatters: "Scheduling is impossible to understand without the state model — every scheduler moves processes between these states.",
    simple: "A process starts as New (being created), becomes Ready (waiting for CPU), moves to Running (using the CPU), might go to Waiting (blocked on I/O), and finally Terminates.",
    technical: "Ready means the process could run but the CPU is busy elsewhere. Running means it is executing on a CPU. Waiting means it is blocked until some event (usually I/O) completes. The transitions are triggered by the scheduler, interrupts, I/O events, and process exit.",
    internal: "The kernel maintains per-process state in the PCB. A dispatch moves Ready→Running (dispatcher loads context). An interrupt or I/O request moves Running→Ready or Running→Waiting. An I/O completion moves Waiting→Ready.",
    keyPoints: [
      "Five states: New, Ready, Running, Waiting, Terminated.",
      "Ready ≠ Running: ready processes are waiting for the CPU.",
      "Waiting means blocked on an event, not on the CPU.",
      "A process can only go Ready→Running (never Waiting→Running directly).",
    ],
    commonMistakes: [
      "Confusing 'ready' (needs CPU) with 'waiting' (needs I/O).",
      "Drawing a direct Waiting→Running transition.",
    ],
    examTips: [
      "Memorize the valid transitions — invalid ones (e.g., Waiting→Running) are common trap answers.",
    ],
    visualizer: "process-states",
  },
  {
    id: "process-lifecycle",
    section: "processes",
    title: "Process Lifecycle",
    shortDescription: "From creation to termination.",
    definition: "The lifecycle is the sequence of states a process passes through from creation (New) to termination (Terminated).",
    whyItMatters: "The lifecycle ties together creation, scheduling, I/O, and termination into one narrative.",
    simple: "A process is born (New), gets admitted (Ready), runs (Running), may wait for I/O (Waiting), comes back (Ready), and finally dies (Terminated).",
    technical: "Creation (fork/exec) builds the process image and PCB. Admission moves it to the ready queue. The short-term scheduler dispatches it. During execution it alternates between CPU and I/O bursts, and finally calls exit(), becoming Terminated and returning its status to its parent.",
    internal: "At each transition the kernel updates the PCB state field, moves the PCB between queues, and (on dispatch) performs a context switch to load the process's saved state.",
    keyPoints: [
      "Lifecycle = New → Ready → Running ⇄ Waiting → Terminated.",
      "A process alternates between CPU and I/O bursts.",
      "exit() transitions Running → Terminated.",
    ],
    commonMistakes: [
      "Thinking a process returns to Running directly from Waiting.",
    ],
    examTips: [
      "Be able to narrate the lifecycle for a short program that does I/O.",
    ],
    visualizer: "process-states",
  },
  {
    id: "pcb",
    section: "processes",
    title: "Process Control Block",
    shortDescription: "The kernel's record card for each process.",
    definition: "The PCB is a per-process data structure that stores everything the OS needs to know about a process: state, program counter, registers, scheduling info, memory info, accounting, and I/O status.",
    whyItMatters: "The PCB is the single source of truth the kernel uses to save and restore a process during context switches.",
    simple: "For every process, the kernel keeps a 'record card'. It lists the process ID, what state it's in, where it was in its code (program counter), the values in its registers, how much CPU it has used, and what files it has open.",
    technical: "The PCB contains: PID, process state, program counter, CPU registers, CPU-scheduling information (priority, queue pointers), memory-management information (base/limit or page tables), accounting (CPU time, limits), and I/O status (open files, devices).",
    internal: "On a context switch, the kernel saves the current CPU registers and program counter into the running process's PCB, then loads the next process's saved values from its PCB back into the CPU.",
    keyPoints: [
      "PID, state, PC, registers, scheduling info, memory info, accounting, I/O status.",
      "The PCB lets the kernel suspend and resume any process.",
      "One PCB per process, stored in kernel memory.",
    ],
    commonMistakes: [
      "Forgetting the program counter is saved in the PCB.",
      "Confusing the PCB with the process image (memory contents).",
    ],
    examTips: [
      "List the PCB fields — a guaranteed memorization question.",
    ],
    visualizer: "pcb",
  },
  {
    id: "context-switching",
    section: "processes",
    title: "Context Switching",
    shortDescription: "How the CPU moves between processes.",
    definition: "A context switch saves the state of the running process and loads the saved state of the next process so execution can continue seamlessly.",
    whyItMatters: "Context switches are the mechanism behind multitasking — and they are pure overhead, so understanding their cost matters.",
    simple: "When the CPU stops running process P1 and starts running P2, it must save P1's registers and program counter, then load P2's. That save-and-load operation is a context switch.",
    technical: "The switch is triggered by an interrupt, a system call, a time-slice expiry, or preemption. The kernel saves the current context into P1's PCB, updates its state, selects the next process, loads its context, and resumes. The time taken is overhead — no useful work happens during the switch.",
    internal: "Steps: (1) save PC and registers to PCB(P1); (2) update P1's state to ready/waiting; (3) scheduler selects P2; (4) load PC and registers from PCB(P2); (5) update memory-management info if needed; (6) resume P2.",
    keyPoints: [
      "Context switch = save current context, load next context.",
      "It is pure overhead — the CPU does no useful work during it.",
      "Triggered by interrupts, syscalls, time-slice expiry, or preemption.",
      "Context is stored in the PCB.",
    ],
    commonMistakes: [
      "Thinking a context switch does useful computation.",
      "Confusing context switch with mode switch (user↔kernel).",
    ],
    examTips: [
      "Define overhead precisely: the time the CPU spends switching rather than executing.",
    ],
    visualizer: "context-switch",
  },
  {
    id: "process-scheduling",
    section: "processes",
    title: "Process Scheduling",
    shortDescription: "Deciding which process gets the CPU.",
    definition: "Scheduling is the policy and mechanism by which the OS selects a ready process to assign to the CPU.",
    whyItMatters: "Scheduling directly determines responsiveness, throughput, and CPU utilization.",
    simple: "Many processes want the CPU at once. The scheduler is the traffic officer that decides who goes next, and the dispatcher actually gives them the CPU.",
    technical: "There are three levels: the long-term scheduler (job admission into memory), the medium-term scheduler (swapping), and the short-term scheduler (CPU selection). The short-term scheduler runs very frequently and must be fast.",
    internal: "The short-term scheduler picks a process from the ready queue (based on the policy), then the dispatcher performs the context switch to run it.",
    keyPoints: [
      "Scheduler decides WHO; dispatcher gives the CPU.",
      "Short-term scheduler runs on every scheduling decision.",
      "Long-term scheduler controls the degree of multiprogramming.",
    ],
    commonMistakes: [
      "Merging the roles of scheduler and dispatcher.",
    ],
    examTips: [
      "Know the three schedulers and what each controls.",
    ],
    visualizer: "scheduling-queues",
  },
  {
    id: "scheduling-queues",
    section: "processes",
    title: "Scheduling Queues",
    shortDescription: "Job queue, ready queue, and device queues.",
    definition: "The OS maintains queues of PCBs: a job queue (all processes), a ready queue (processes waiting for CPU), and device/waiting queues (processes waiting for I/O).",
    whyItMatters: "Queues are the concrete data structures that implement the state model.",
    simple: "Processes are organized into lines: a ready line for those wanting the CPU, and separate waiting lines for each I/O device. A process moves between lines as its state changes.",
    technical: "The ready queue is typically a linked list of PCBs. Each I/O device has its own waiting queue. The scheduler picks the next PCB from the ready queue; when I/O completes, the kernel moves that PCB from the device queue back to the ready queue.",
    internal: "A newly admitted process enters the ready queue. On dispatch it leaves the ready queue. On an I/O request it enters a device queue. On I/O completion it re-enters the ready queue. On termination it leaves all queues.",
    keyPoints: [
      "Job queue = all processes in the system.",
      "Ready queue = processes waiting for the CPU.",
      "Device/waiting queues = processes waiting for I/O.",
      "Processes migrate between queues as state changes.",
    ],
    commonMistakes: [
      "Thinking there is a single queue for everything.",
    ],
    examTips: [
      "Be able to sketch the queue migration diagram.",
    ],
    visualizer: "scheduling-queues",
  },
  {
    id: "scheduler-vs-dispatcher",
    section: "processes",
    title: "Scheduler vs Dispatcher",
    shortDescription: "Decision vs action.",
    definition: "The scheduler selects the next process to run; the dispatcher gives the CPU to that process by loading its context.",
    whyItMatters: "These two roles are frequently tested and often conflated.",
    simple: "The scheduler is the manager choosing who goes next; the dispatcher is the doorman who actually escorts the chosen process onto the CPU.",
    technical: "Scheduling is a decision (which process?), done by the scheduler using the policy. Dispatching is the mechanism: switching context, switching to user mode, and jumping to the process's program counter. Dispatch latency is the time for the dispatcher to stop one process and start another.",
    internal: "After the scheduler picks P2, the dispatcher: saves P1's context into its PCB, loads P2's context, flushes/updates memory state, and jumps to P2's PC.",
    keyPoints: [
      "Scheduler = decides which process runs next.",
      "Dispatcher = actually transfers CPU control.",
      "Dispatch latency = time to stop one process and start the next.",
    ],
    commonMistakes: [
      "Using the terms interchangeably in answers.",
    ],
    examTips: [
      "Define dispatch latency — it is a short, high-yield definition.",
    ],
    visualizer: "scheduler-dispatcher",
  },
  {
    id: "process-creation",
    section: "processes",
    title: "Process Creation",
    shortDescription: "How new processes are born.",
    definition: "Process creation is the procedure of building a new process image, allocating resources, and creating its PCB.",
    whyItMatters: "Every process in the system (except the very first) was created by another process.",
    simple: "A process is created when a parent process asks the OS to make a copy of itself (fork). The child can then replace its code with a new program (exec).",
    technical: "On POSIX systems, fork() creates a child that is a copy of the parent; exec() replaces the child's memory with a new program. The OS allocates a PID, a PCB, an address space, and initializes the child's resources.",
    internal: "fork() duplicates the parent's address space (often with copy-on-write), copies the PCB (new PID, reset accounting), and places the child in the ready queue. exec() loads a new program image into the child's address space.",
    keyPoints: [
      "fork() creates a child copy; exec() loads a new program.",
      "Each process gets a unique PID and its own PCB.",
      "Resources can be shared or duplicated (parent/child).",
      "Child is placed in the ready queue after creation.",
    ],
    commonMistakes: [
      "Believing the child and parent share the same PID.",
    ],
    examTips: [
      "Know fork/exec and that PID 0/1 are special (init).",
    ],
    visualizer: "process-tree",
  },
  {
    id: "process-termination",
    section: "processes",
    title: "Process Termination",
    shortDescription: "How processes exit and clean up.",
    definition: "Termination ends a process's execution and releases its resources back to the OS.",
    whyItMatters: "Clean termination prevents resource leaks and lets parents know how their children ended.",
    simple: "A process ends either by itself (calling exit) or because someone kills it (a parent or the OS). Its memory and open files are freed, and its parent is told how it finished.",
    technical: "Termination can be voluntary (exit()) or involuntary (killed by parent, fatal error, or OS due to resource limits). The OS reclaims memory, closes open files, removes the PCB from queues, and returns an exit status to the parent.",
    internal: "The kernel deallocates the process's address space, releases locks and files, removes the PCB from all queues, and stores the exit code so the parent can collect it via wait(). If the parent doesn't collect it, the process becomes a zombie.",
    keyPoints: [
      "exit() → voluntary; kill → involuntary.",
      "Resources are reclaimed: memory, files, PCB.",
      "Exit status is returned to the parent via wait().",
      "Un-collected children become zombies; orphans are adopted by init.",
    ],
    commonMistakes: [
      "Confusing zombie (terminated, not collected) with orphan (parent died).",
    ],
    examTips: [
      "Zombie vs orphan is a classic definition pair.",
    ],
  },
  {
    id: "parent-child-processes",
    section: "processes",
    title: "Parent & Child Processes",
    shortDescription: "The fork relationship between processes.",
    definition: "A parent process creates a child process; together they form a hierarchy. The child may share resources with, or be independent of, its parent.",
    whyItMatters: "The parent-child relationship is the basis of process trees, job control, and inter-process coordination.",
    simple: "When a process creates another, the creator is the parent and the new one is the child. The child inherits a copy of the parent's resources and can later run its own code.",
    technical: "After fork(), the child gets its own PID and address space (copy of the parent's), but may share open file descriptors. The parent can wait() for the child or run concurrently. Termination options: the parent may terminate children (cascading) or let them continue.",
    internal: "The kernel links the child's PCB to the parent's PCB (ppid). If the parent exits first, the child becomes an orphan and is re-parented to init (PID 1). If the child exits first and the parent never waits, the child becomes a zombie.",
    keyPoints: [
      "Parent creates child via fork().",
      "Child has its own PID and address space.",
      "Parent may wait() for children or run concurrently.",
      "Orphans are adopted by init; zombies await the parent.",
    ],
    commonMistakes: [
      "Assuming parent and child share all memory (they share file descriptors, not general memory).",
    ],
    examTips: [
      "Explain ppid, orphan, and zombie in one short answer.",
    ],
    visualizer: "process-tree",
  },
  {
    id: "process-trees",
    section: "processes",
    title: "Process Trees",
    shortDescription: "The hierarchy of processes in a system.",
    definition: "A process tree is the hierarchical structure formed by parent-child relationships, rooted at the init process.",
    whyItMatters: "The tree shows the genealogy of processes and how the system bootstraps its process population.",
    simple: "At boot, the first process (init) starts. Every process it creates becomes its child, and those children create more. The result is a family tree of processes.",
    technical: "On Linux, init (PID 1) is the root of the tree; systemd/launchd are its descendants. When a parent terminates, the OS walks the tree to re-parent orphans. Tools like pstree visualize this hierarchy.",
    internal: "Each PCB stores a parent PID pointer. The kernel uses these pointers to enforce cascading termination and to re-parent orphans when a parent dies.",
    keyPoints: [
      "Rooted at init (PID 1).",
      "Each PCB stores its parent's PID.",
      "pstree/Process Explorer visualize the tree.",
      "Parent termination can cascade to children.",
    ],
    commonMistakes: [
      "Thinking the tree has no root or multiple roots.",
    ],
    examTips: [
      "Be able to draw a small process tree given a fork() sequence.",
    ],
    visualizer: "process-tree",
  },
  {
    id: "process-operations",
    section: "processes",
    title: "Process Operations",
    shortDescription: "Create, schedule, execute, terminate.",
    definition: "The OS performs a fixed set of operations on processes: creation, scheduling, execution, and termination.",
    whyItMatters: "These four operations summarize the entire process-management subsystem.",
    simple: "The OS can create a process, choose it to run (schedule), let it execute, and finally terminate it — and it repeats these operations constantly for every process.",
    technical: "Creation sets up the image and PCB. Scheduling places the PCB in the ready queue and later selects it. Execution is the dispatcher running it (context switching as needed). Termination cleans up resources and returns status.",
    internal: "Each operation has a kernel entry point (fork, scheduler, dispatcher, exit). The PCB state field is updated at every step, and queue membership changes accordingly.",
    keyPoints: [
      "Four core operations: create, schedule, execute, terminate.",
      "Each maps to kernel routines (fork/sched/dispatch/exit).",
    ],
    commonMistakes: [
      "Listing 'wait' as a process operation rather than a parent operation.",
    ],
    examTips: [
      "Use these four operations to structure longer essay answers.",
    ],
    visualizer: "process-tree",
  },
];
